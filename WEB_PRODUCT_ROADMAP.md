# 网页产品扩展实施路线

## 一、项目背景与共同约束

### 项目位置

- 网页：`E:\小程序开发\herb_share-main\herb_share`
- 微信小程序：`E:\小程序开发\miniprogram-1`
- 管理端：`E:\小程序开发\mp-admin`

### 产品定位

网页和“一起来玩哈皮吗”小程序属于同一产品，不是互相推荐的两个产品：

- 小程序负责随手查询、轻量计算、资料录入和微信内入口。
- 网页负责复杂检索、大屏展示、长期规划、公开分享和玩家互动。
- 已有草药、烹饪、换卡互助广场继续保留在网页端。

### 视觉与交互规范

- 延续现有浅色羊皮纸风格：米白背景、深绿色主色、金色点缀。
- 优先适配手机，同时充分利用桌面端宽屏。
- 所有新增页面必须有返回首页或主导航入口。
- 弹窗在桌面端居中，在手机端使用底部抽屉。
- 不改变已有互助广场的数据结构和发布逻辑。
- 不在代码里重复维护小程序已有资料；应提供转换脚本或共享数据源。

### 数据与安全原则

- 设备本地偏好可以使用 `localStorage`。
- 需要跨设备同步、公开分享或多人互动的数据应使用 Supabase。
- 用户生成内容必须具备删除、举报、屏蔽、限流和后台审核能力。
- 第一阶段不做私信、群聊、实时聊天室和无限楼中楼评论。
- 不公开微信号、QQ、手机号等敏感联系方式。

---

## 二、推荐实施顺序

1. 完善综合资料检索站
2. 完善我的收藏一览
3. 资源规划工作台
4. 抽卡与概率实验室
5. 魔法创作广场基础版
6. 创作广场互动与治理
7. 评估卡组分享社区
8. 评估资料投稿与纠错

其中 1、2 已有第一版。新对话开始前必须先检查现有文件和未提交改动，不要从零重写。

---

## 三、综合资料检索站

### 当前状态

第一版已实现：

- 页面：`catalog.html`
- 样式：`catalog.css`
- 逻辑：`catalog.js`
- 数据：`catalog-data.json`
- 数据生成：`tools/build-catalog.js`
- 已接入服饰、卡牌皮肤、魔杖、动物，共 296 条。
- 已支持搜索、分类筛选、卡片列表和详情弹窗。

### 下一阶段目标

继续接入：

- 返场时间线
- 上新时间线
- 赛季图鉴
- 平衡调整档案
- 魔盒或其他收藏资料

### 功能要求

- 支持跨所有模块统一搜索。
- 支持按分类、年份、来源和稀有度筛选。
- 搜索“凤凰”等关键词时能命中不同分类的相关资料。
- URL 支持保存搜索条件，例如 `catalog.html?q=凤凰&type=cardskin`。
- 详情页提供稳定的可复制链接。
- 处理同名项目、男女款名称和别名。
- 图片失败时显示分类图标，不出现破图。

### 数据要求

- 扩展 `tools/build-catalog.js`，不要手动编辑生成后的 JSON。
- 标准字段至少包括：`id`、`category`、`name`、`year`、`date`、`source`、`image`、`aliases`、`details`。
- 同一资料的 `id` 必须稳定，数据重新生成后不能导致收藏记录全部失效。

### 验收标准

- 所有来源模块数量与小程序本地数据一致。
- 搜索、筛选和 URL 参数组合正常。
- 390px 手机宽度无横向滚动。
- 详情弹窗可关闭、可按 Escape 关闭。
- 数据生成脚本可重复运行。

### 新对话任务描述

> 继续完善网页项目的“综合资料检索站”。网页位于 `E:\小程序开发\herb_share-main\herb_share`，小程序位于 `E:\小程序开发\miniprogram-1`。先检查 `catalog.html`、`catalog.js`、`catalog.css`、`catalog-data.json` 和 `tools/build-catalog.js`，保留当前实现。在现有服饰、卡牌皮肤、魔杖、动物基础上，接入返场、上新、赛季图鉴和平衡调整档案；增加年份、来源筛选、URL 搜索参数和可复制详情链接。不要手工维护重复数据，扩展生成脚本，并验证移动端布局和数据数量。

---

## 四、我的收藏一览

### 当前状态

第一版已实现：

- 页面：`collection.html`
- 样式：`collection.css`
- 逻辑：`collection.js`
- 与资料检索共用 `catalog-data.json`。
- 收藏键：`hogwarts_catalog_owned_v1`。
- 支持总收藏率、分类进度、已拥有/未拥有筛选和搜索。

### 下一阶段目标

- 收藏数据导出与导入。
- 防止资料更新后收藏记录丢失。
- 可选的跨设备同步。
- 收藏清单分享图。
- 缺失清单一键生成。

### 功能要求

- 导出 JSON 备份文件。
- 从 JSON 文件恢复，导入前显示数量并确认。
- 生成“我的收藏进度”分享图。
- 生成“我还缺这些”分享图，可选分类。
- 提供最近新增但尚未标记的资料。
- 支持按年份查看收藏率。

### 跨设备同步建议

第一版继续本地保存。第二版若接入同步：

- 使用匿名或明确的用户身份方案。
- 表建议：`collection_profiles`、`collection_items`。
- 本地记录与云端记录需要合并，不能直接覆盖。
- 提供“仅本机”和“同步到云端”的清晰状态。

### 验收标准

- 导出后清空浏览器数据，再导入可恢复收藏。
- 新增资料后旧收藏仍有效。
- 分享图中文不截断。
- 未登录时原有本地功能完全可用。

### 新对话任务描述

> 完善网页项目的“我的收藏一览”。先检查 `collection.html`、`collection.js`、`collection.css`、`catalog-data.json` 和 `catalog.js`，不要重写第一版。增加收藏 JSON 导入/导出、按年份统计、最近新增、缺失清单和收藏进度分享图。保持未登录时使用 `localStorage`，确保资料数据重新生成后稳定 ID 不会使旧收藏失效。暂时不要强制登录或实现公开个人主页。

---

## 五、资源规划工作台

### 定位

不要复制小程序里的资源计算器。小程序负责快速计算，网页应当成为长期规划和多方案比较工具。

### 可复用的小程序代码

- `pages/resourceCalc/resourceCalc.js`
- `pages/resourceCalc/resourceCalc.wxml`
- 日常、限时活动和资源计算规则应从现有代码提取。

### 第一版范围

1. 当前资源
   - 金币、宝石、钥匙等当前库存。
   - 支持自定义资源类型。

2. 目标资源
   - 目标数量。
   - 目标日期。
   - 可添加多个目标。

3. 收益计划
   - 勾选日常和限时活动。
   - 设置参与频率和个人实际收益。
   - 支持收入与计划支出。

4. 结算结果
   - 目标日期预计资源。
   - 预计达成日期。
   - 目标缺口。
   - 最低需要完成的活动建议。

5. 多方案比较
   - 至少支持三套方案。
   - 示例：休闲、全勤、月卡。
   - 对比最终资源、达成日期、付费成本和活动数量。

6. 日历和图表
   - 每日或每周资源变化。
   - 收入、支出和净增长趋势。
   - 标注活动结算和目标达成节点。

7. 保存与导出
   - 本地保存多个方案。
   - 导入/导出 JSON。
   - 导出 CSV 或图片报告。

### 数据模型建议

```js
{
  id,
  name,
  startDate,
  endDate,
  balances: { 金币: 0, 宝石: 0 },
  goals: [{ resource, amount, targetDate }],
  activities: [{ id, frequency, rewards, enabled }],
  expenses: [{ date, name, resources }]
}
```

### 暂不实现

- 不做复杂账号系统。
- 不做真实支付。
- 不自动承诺未来活动奖励一定准确。
- 不把管理员编辑入口暴露给普通用户。

### 验收标准

- 与小程序相同输入时基础结算结果一致。
- 三个方案可同时比较。
- 收入减支出的净值正确。
- 日期边界、每周频率和一次性活动计算正确。
- 手机端可完成录入，桌面端能看到完整对比表。

### 新对话任务描述

> 在网页项目 `E:\小程序开发\herb_share-main\herb_share` 中实现“资源规划工作台”，并复用小程序 `E:\小程序开发\miniprogram-1\pages\resourceCalc` 的活动和计算规则。它不能只是复制小程序计算器，要实现当前库存、多个目标、收入与支出、多方案对比、预计达成日期、目标缺口、资源趋势图、日历节点、本地保存和 JSON/CSV 导出。先提取共享计算数据并为核心计算写验证用例，再接入现有网页首页导航和视觉风格。

---

## 六、抽卡与概率实验室

### 定位

用于解释概率、模拟保底和比较预算方案，不只是播放抽卡动画。

### 可复用的小程序代码

- `pages/gacha/gacha.js`
- `pages/gachaResult/gachaResult.js`
- `pages/gembox/gembox.js`
- 相关概率配置和保底规则。

### 第一版范围

- 自定义抽取次数或预算。
- 选择卡池或概率配置。
- 单次模拟和批量模拟。
- 展示平均值、中位数、分位数和最差区间。
- 展示概率分布直方图与累计概率。
- 计算达到目标概率需要的抽数或预算。
- 比较不同策略或不同卡池。
- 保存实验配置和导出结果。

### 必须明确展示

- 模拟次数。
- 使用的概率和保底规则。
- 这是统计模拟，不保证个人实际结果。
- 数据更新时间和来源。

### 验收标准

- 固定随机种子时结果可复现。
- 大样本结果接近理论概率。
- 保底触发边界正确。
- 图表在手机和桌面端都可读。
- 运行大量模拟时页面不卡死，必要时使用 Web Worker。

### 新对话任务描述

> 在网页项目中实现“抽卡与概率实验室”。复用小程序 `pages/gacha`、`pages/gachaResult`、`pages/gembox` 和相关配置中的概率及保底规则。支持自定义抽数/预算、批量模拟、理论概率、平均值、中位数、分位数、累计概率、保底分析、多方案比较、图表和结果导出。核心计算与界面分离，加入固定随机种子和验证用例；大量模拟使用不会阻塞页面的实现。明确显示规则来源、更新时间和“模拟不代表实际结果”。

---

## 七、魔法创作广场

### 产品目标

让用户除了查资料之外，还有发布、浏览和回访的理由。第一版是结构化作品社区，不是聊天室。

### 内容分类

- 玩家相册
- 穿搭展示
- 绘画与手作
- 梗图趣闻
- 攻略心得
- 其他创作

### 第一版范围

1. 浏览
   - 最新、精选两个信息流。
   - 分类和标签筛选。
   - 作品详情页。

2. 发布
   - 标题、正文、分类、标签。
   - 1～6 张图片。
   - 作者昵称。
   - 可关联资料库中的服饰、魔杖、卡皮或动物。

3. 作者操作
   - 删除自己的作品。
   - 编辑标题、正文和标签。
   - 查看自己的发布。

4. 基础治理
   - 举报作品。
   - 屏蔽作品。
   - 发布频率限制。
   - 图片大小、数量和格式限制。
   - 管理后台隐藏、删除和设为精选。

### Supabase 表建议

```sql
creative_posts (
  id, owner_id, nickname, category, title, body,
  tags, linked_catalog_ids, status, featured,
  created_at, updated_at
)

creative_assets (
  id, post_id, storage_path, sort_order, width, height, created_at
)

creative_reports (
  id, post_id, reporter_id, reason, status, created_at
)
```

图片存放在独立 Storage bucket，不要把 Base64 写入数据库。

### 内容安全要求

- 发布前展示社区规则。
- 图片和文字需要内容安全处理方案。
- 未完成审核能力前，可先采用“发布后仅自己可见，管理员审核后公开”。
- 记录审核状态：`pending`、`published`、`hidden`、`rejected`。
- 普通用户不能修改 `featured` 和审核状态。
- 删除尽量采用软删除，便于处理举报。

### 暂不实现

- 私信。
- 实时聊天。
- 群聊。
- 关注与粉丝系统。
- 用户联系方式公开展示。
- 评论和楼中楼。

### 验收标准

- 手机端可以选择并发布多张图片。
- 未授权用户不能编辑或删除他人作品。
- 被隐藏或未通过审核的内容不会出现在公共列表。
- 图片上传失败可重试，不产生孤立数据库记录。
- 举报后管理端能够定位作品并处理。

### 新对话任务描述

> 在现有网页项目中设计并实现“魔法创作广场”第一版。它是作品发布社区，不是聊天室。支持玩家相册、穿搭、绘画手作、梗图和攻略分类；作品包含标题、正文、标签、昵称和 1～6 张图片，并可关联 `catalog-data.json` 中的资料。使用 Supabase 表和 Storage，必须实现作者删除/编辑、举报、屏蔽、频率限制、审核状态和管理员精选。先设计 SQL migration、RLS 和上传失败回滚，再实现移动端优先的列表、详情和发布页。暂不做评论、私信、群聊、关注和联系方式展示。

---

## 八、创作广场互动与治理

该阶段必须在创作广场基础发布和审核稳定后开始。

### 第一阶段互动

- 点赞。
- 收藏作品。
- 精选作品。
- 作者主页只展示公开作品，不展示敏感信息。

### 第二阶段可评估

- 一级评论。
- 作者回复。
- 评论举报。
- 评论删除。
- 关键词过滤和频率限制。

### 表建议

```sql
creative_likes (post_id, user_id, created_at)
creative_bookmarks (post_id, user_id, created_at)
creative_comments (id, post_id, user_id, nickname, body, status, created_at)
moderation_actions (id, target_type, target_id, action, reason, operator_id, created_at)
```

### 验收标准

- 同一用户对同一作品只能点赞一次。
- 点赞数不能由客户端任意修改。
- 隐藏作品的互动数据不会泄露作品内容。
- 管理操作保留审计记录。

### 新对话任务描述

> 为已经完成基础发布与审核的“魔法创作广场”增加互动和治理。第一阶段只做点赞、收藏、精选和公开作者作品列表；实现唯一约束、RLS、计数一致性和管理操作审计。评估稳定后再实现一级评论与作者回复，并加入评论举报、删除、关键词过滤和频率限制。禁止实现私信、实时聊天、群聊和联系方式展示。

---

## 九、卡组分享社区（待评估）

### 价值

- 可复用小程序卡组编辑器。
- 卡组可关联回响、模式、段位、核心卡和替换卡。
- 与作品广场的攻略内容有较强协同。

### 建议

不要立即建立第二套社区系统。优先考虑将“卡组”作为创作广场的一种结构化作品类型：

- 作品正文保存心得。
- 结构化字段保存卡组、回响、模式和替换卡。
- 共用点赞、收藏、举报、审核和作者体系。

### 评估条件

- 创作广场已有稳定用户发布。
- 用户确实频繁发布卡组类内容。
- 管理端审核流程稳定。

### 新对话任务描述

> 评估将小程序卡组编辑器接入网页创作广场，而不是另建一套社区。检查 `pages/deck` 和 `pages/deck_edit` 的数据结构，设计“卡组作品”结构化字段、卡组图片生成、回响/模式/段位筛选和替换卡说明。复用创作广场已有的作者、点赞、收藏、举报和审核体系。先提交技术设计和迁移方案，不要直接实现完整社区。

---

## 十、资料投稿与纠错（待评估）

### 价值

- 让玩家补充遗漏资料、图片、来源和版本变化。
- 减少维护者手工收集数据的压力。

### 风险

- 不能让投稿直接修改正式资料。
- 必须保留修改前后内容、来源和审核记录。
- 图片版权和来源需要说明。

### 建议流程

1. 用户从资料详情点击“纠错/补充”。
2. 选择字段并填写建议值。
3. 提供来源链接或说明。
4. 投稿进入待审核状态。
5. 管理员比较差异后接受或拒绝。
6. 接受后由正式数据维护流程写入源数据。

### 新对话任务描述

> 为综合资料检索站设计“资料投稿与纠错”流程。投稿不能直接修改正式资料。设计投稿表、字段级差异、来源说明、图片附件、审核状态、管理员对比界面和审核记录；接受投稿后应进入现有资料源维护流程，而不是只修改生成后的 `catalog-data.json`。先完成数据模型、RLS、审核流程和管理端原型，再决定是否开放给用户。

---

## 十一、跨对话协作规则

每个新对话都应明确要求：

1. 先运行 `git status --short`，保护现有未提交改动。
2. 先阅读与任务相关的现有文件，不要从零创建重复页面。
3. 不随意修改其他功能的数据结构。
4. 新增公共数据结构时记录版本和迁移方式。
5. 完成后执行语法检查、数据数量检查和适度的交互验证。
6. 汇报修改文件、实现范围、暂未完成项和需要部署的数据库变更。
7. 不自动提交、推送或发布，除非该对话明确获得授权。

## 十二、建议的对话拆分

- 对话 A：完善综合资料检索。
- 对话 B：完善收藏一览与备份。
- 对话 C：资源规划工作台。
- 对话 D：抽卡与概率实验室。
- 对话 E：创作广场数据库、Storage、RLS 和审核设计。
- 对话 F：创作广场前端列表、详情和发布。
- 对话 G：管理端内容审核与举报处理。
- 对话 H：互动能力（点赞、收藏、精选）。

创作广场的 E、F、G 存在依赖：先完成 E 的数据设计，再并行开发 F 和 G；H 最后进行。
